syntax = "proto3";
package ShareRing.shareledger.electoralbk;

import "gogoproto/gogo.proto";

option go_package = "github.com/ShareRing/Shareledger/x/electoralbk/types";

// GenesisState defines the electoralbk module's genesis state.
message GenesisState {
    repeated string addresses = 1 [(gogoproto.nullable) = true];
    // this line is used by starport scaffolding # genesis/proto/state
}
