syntax = "proto3";
package shareledger.swap;

import "common/common.proto";

option go_package = "github.com/sharering/shareledger/x/swap/types";

message RequestedIn {
  string address = 1; 
  map<string, bool> txHashes = 2; // bool is not used, just for map value.
}
