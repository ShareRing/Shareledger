syntax = "proto3";
package shareledger.swap;

option go_package = "github.com/sharering/shareledger/x/swap/types";

message Batch {
  uint64 id = 1;
  string signature = 2;
  repeated uint64 reqIDs = 3;
  string status = 4;
  string network = 5;
}