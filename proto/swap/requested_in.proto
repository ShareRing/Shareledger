syntax = "proto3";
package shareledger.swap;

import "common/common.proto";

option go_package = "github.com/sharering/shareledger/x/swap/types";

message RequestedIn {
  string address = 1; 
  map<string, common.Empty> txHashes = 2; //Could not use google.protobuf.Empty, which misses Size method generated by ignite
}
